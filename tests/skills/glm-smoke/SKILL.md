---
name: glm-smoke
description: A tiny local skill used to verify that Codex Desktop can discover and follow a project skill with a custom provider.
---

# GLM smoke skill

When this skill is invoked, explain that the local skill was discovered, then use the configured `glm_smoke_echo` MCP tool if it is available. Pass a short message and report the returned `MCP_OK:` value. Do not modify `README.md`.
